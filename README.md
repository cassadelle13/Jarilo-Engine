# ⚡ Jarilo Ecosystem - Multi-Agent Orchestration Engine

Jarilo — это высокопроизводительная мультиагентная платформа, спроектированная для автоматизации сложных рабочих процессов. Система построена на базе **FastAPI-ядра**, **Deterministic LLM Planner** и **Isolated Containerized Executors**.

## 🏗️ Архитектурный обзор (System Architect Perspective)

Проект Jarilo спроектирован с упором на **модульность**, **наблюдаемость** и **безопасную изоляцию** исполняемого кода. Архитектура разделена на три ключевых домена:

### 1. Координационный центр (The Brain)
Расположенный в директории `brain/`, этот компонент является "мозгом" системы.
- **Технологический стек**: FastAPI + LangChain + TinyDB.
- **Design Decisions**: Использование паттернов **Adapter** и **Strategy** обеспечивает Vendor-Agnostic подход к LLM-провайдерам и хранилищам. (См. [ADR-001: Database Choice](ADR-001-Database-Choice.md))
- **Оркестрация**: `planner.py` отвечает за декомпозицию задач в **Directed Acyclic Graph (DAG)**, а `executor.py` управляет жизненным циклом агентов.

### 2. Среда исполнения (Agent Runtime)
Агенты функционируют в полностью изолированных Docker-контейнерах.
- **Изоляция**: Каждый процесс выполняется в уникальном `workspace`, что предотвращает **Supply Chain Attacks** и обеспечивает безопасность хост-системы.
- **Инструментарий**: Нативная поддержка выполнения Bash и Python кода с использованием **Structured Output Parsing** (Markdown-ответы).
- **Обратная связь**: Метаданные исполнения и логи возвращаются в Brain по HTTP для дальнейшего анализа планировщиком.

### 3. Реактивная событийная модель (Streaming)
Система обеспечивает полную прозрачность выполнения через **Server-Sent Events (SSE)**.
- **Event Dispatcher**: Публикует типизированные события (`PLAN_GENERATED`, `STEP_STARTED`, `TASK_COMPLETED`) в реальном времени.
- **Надежность**: Встроенные механизмы heartbeats и автоматическая очистка очередей гарантируют стабильность потоков данных при высоких нагрузках.

## 🎯 Ключевые возможности

- **Deterministic Planning**: Преобразование высокоуровневых промптов в детализированные, исполняемые планы (DAG).
- **Real-time Observability**: Мониторинг каждого шага выполнения через стриминг событий.
- **Extensibility**: Простая интеграция новых инструментов через систему плагинов (`plugins/`).
- **Secure Execution**: Глубокая изоляция рабочих сред и отсутствие конфиденциальных данных в кодовой базе.

## 📂 Документация и ресурсы

Для более глубокого погружения в специфику системы обратитесь к следующим документам:
- [ADR-001: Database Choice](ADR-001-Database-Choice.md) — Обоснование выбора TinyDB для MVP.
- `STREAMING_ARCHITECTURE.md` — Детальное описание протоколов событий.
- `JARILO_SYSTEMS.md` — Подробный разбор внутренних компонентов и паттернов.

---
