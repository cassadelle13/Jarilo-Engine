# ⚡ Jarilo Ecosystem - Multi-Agent Orchestration Engine

Jarilo — это высокопроизводительная мультиагентная платформа, спроектированная для автоматизации сложных рабочих процессов. Система построена на базе FastAPI-ядра, интеллектуального LLM-планировщика и изолированных контейнеризированных исполнителей.

## 🏗️ Архитектурный обзор (System Architect Perspective)

Проект Jarilo спроектирован с упором на **модульность**, **наблюдаемость** и **безопасную изоляцию** исполняемого кода. Архитектура разделена на три ключевых домена:

### 1. Координационный центр (The Brain)
Расположенный в директории `brain/`, этот компонент является "мозгом" системы.
- **Технологический стек**: FastAPI + LangChain + TinyDB.
- **Паттерны**: Реализованы паттерны Dependency Injection, Strategy, Observer и Adapter для обеспечения гибкости при замене LLM-провайдеров или механизмов хранения.
- **Оркестрация**: `planner.py` отвечает за декомпозицию задач, а `executor.py` управляет жизненным циклом агентов.

### 2. Среда исполнения (Agent Runtime)
Агенты функционируют в полностью изолированных Docker-контейнерах.
- **Изоляция**: Каждый процесс выполняется в уникальном `workspace`, что предотвращает конфликты и обеспечивает безопасность хост-системы.
- **Инструментарий**: Нативная поддержка выполнения Bash и Python кода с автоматическим парсингом Markdown-ответов.
- **Обратная связь**: Метаданные исполнения и логи возвращаются в Brain по HTTP для дальнейшего анализа планировщиком.

### 3. Реактивная событийная модель (Streaming)
Система обеспечивает полную прозрачность выполнения через SSE (Server-Sent Events).
- **Event Dispatcher**: Публикует типизированные события (`PLAN_GENERATED`, `STEP_STARTED`, `TASK_COMPLETED`) в реальном времени.
- **Надежность**: Встроенные механизмы heartbeats и автоматическая очистка очередей гарантируют стабильность потоков данных при высоких нагрузках.

## 🎯 Ключевые возможности

- **Автономное планирование**: Преобразование высокоуровневых промптов в детализированные, исполняемые планы.
- **Real-time Observability**: Мониторинг каждого шага выполнения через стриминг событий.
- **Расширяемая экосистема**: Простая интеграция новых инструментов через систему плагинов (`plugins/`).
- **Enterprise-ready Security**: Глубокая изоляция рабочих сред и отсутствие конфиденциальных данных в кодовой базе.

## 📂 Документация и ресурсы

Для более глубокого погружения в специфику системы обратитесь к следующим документам:
- `STREAMING_ARCHITECTURE.md` — детальное описание протоколов событий.
- `JARILO_SYSTEMS.md` — подробный разбор внутренних компонентов и паттернов.

---

**Vision:** Jarilo стремится стать связующим звеном между мощью современных LLM и безопасным исполнением кода в реальных производственных средах.

**Built with ❤️ by the Jarilo Team**
